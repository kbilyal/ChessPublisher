"use strict";

// Beta preview configuration. This file is public by design and MUST never
// contain organizer credentials, API tokens, Cloudflare account IDs or secrets.
window.ChessPublisherHubConfig=Object.freeze({
  apiBase:"",
  fixturePath:"sample-tournament.json",
  previewFixture:true
});
